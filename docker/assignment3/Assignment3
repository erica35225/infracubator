# Run stage

1. build an image using given dockerfile in the same directory.
   docker build -t erica7036/go-app docker/assignment3

2. Run the image
   docker run -d -p 8080:8080 erica7036/go-app

3. Make a curl request and verify the output
   curl localhost:8080